
version: '3.9'

services:
  image: ghcr.io/samoht89/website1:latest
  image-carousel:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - /mnt/bygg:/mnt/bygg:ro
    restart: unless-stopped
